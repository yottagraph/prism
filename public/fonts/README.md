# Fonts

The app now uses open-license web fonts loaded in `assets/fonts.css`:

- `Space Grotesk` (primary/headline/brand)
- `Space Mono` (mono/data text)

No commercial font files are required in this directory.
